<h1>Listing reminders</h1>

<table>
  <tr>
    <th>Text</th>
    <th>Notify at</th>
  </tr>

<% @reminders.each do |reminder| %>
  <tr>
    <td><%= reminder.text %></td>
    <td><%= reminder.notify_at.strftime("%Y-%m-%d %H:%M") %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Reminder', new_reminder_path %>
